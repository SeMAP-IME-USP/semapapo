[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "semapapo"
version = "1.1.1"
description = "semapapo"
authors = [{ name = "potalej" }]
readme = "README.md"
dependencies = []

[project.scripts]
semapapo = "semapapo.semapapo:semapapo"